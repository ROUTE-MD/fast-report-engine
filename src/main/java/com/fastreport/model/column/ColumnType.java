package com.fastreport.model.column;

/** Data types supported by column definitions, controlling formatting and alignment. */
public enum ColumnType {
    STRING,
    INTEGER,
    DECIMAL,
    DATE,
    CURRENCY,
    PERCENTAGE,
    BOOLEAN
}
